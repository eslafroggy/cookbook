<h1>Recipes index</h1>



<% @recipes.each do |recipe| %>
  <p>
    <%= link_to recipe.title, recipe_path(recipe.id) %> |
    <%= link_to "Edit", edit_recipe_path(recipe.id) %> |
    <%= link_to "Delete", delete_recipe_path(recipe.id), :method => :delete %>
  </p>
<% end %>